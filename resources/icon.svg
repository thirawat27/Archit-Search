<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Magnifying glass circle - maximum size -->
  <circle cx="10" cy="10" r="9" stroke="currentColor" stroke-width="2" fill="none"/>
  
  <!-- Magnifying glass handle -->
  <line x1="17" y1="17" x2="23" y2="23" stroke="currentColor" stroke-width="3" stroke-linecap="round"/>
  
  <!-- Code </> inside the magnifying glass lens - more spacing -->
  <!-- Left bracket < (moved left) -->
  <path d="M7 8L3.5 10L7 12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
  
  <!-- Right bracket > (moved right) -->
  <path d="M13 8L16.5 10L13 12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
  
  <!-- Slash / (centered) -->
  <line x1="11.5" y1="6.5" x2="8.5" y2="13.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
</svg>
